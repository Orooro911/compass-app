/**
 * WIP: dashboard onboarding progress UI preview.
 * Set to `false` to hide the bar (no other code changes needed).
 * Remove the preview component import from `page.tsx` when this ships for real.
 */
export const SHOW_ONBOARDING_PROGRESS_PREVIEW = true;
